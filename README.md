# Название проекта
Bankwidget

## Использование
mask_account_card(type_account_card: str) -> str:
    """Эта функция для счета и карты и названия"""

 get_date(data_str: str) -> str:
    """Эта функция вазвращает дату в заданном формате"""

t_mask_card_number(card_number: str) -> str:
    """Эта функция возвращает замаскированный номер карты"""

get_mask_account(account_number: str) -> str:
    """Эта функция возвращает замаскированный номер счета"""

filter_by_state(list_bank_dictionary, state='EXECUTED'):
    """Эта функция предназначена для фильтрации словаря""

sort_by_date(list_bank_dictionary, reverse=True):
    """Эта функция предназначена для сортировки словаря"""



### Установка зависимостей
Для установки зависимостей, выполните команду:


### Зачем вы разработали этот проект?
Чтобы был.


## Команда проекта
Алманбетова Енлик 
enlik-92@mail.ru


